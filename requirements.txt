# Django 核心
Django>=5.2.5,<6.0
pymysql>=1.1.0

# HTTP 請求（LINE Pay / SMS）
requests>=2.31.0

# 圖片處理（ImageField 必要依賴）
Pillow>=10.0.0

# 富文本編輯器
django-summernote>=0.8.20

# 環境變數
python-dotenv>=1.0.0

# 生產 WSGI
gunicorn>=21.2.0

# 社群登入
social-auth-app-django>=5.4.0

# 可選：設定 SENTRY_DSN 後才會啟用
sentry-sdk>=2.0.0
